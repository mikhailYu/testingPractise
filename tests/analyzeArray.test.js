const analyze = require("../inputs/analyzeArrayInput");
test("Returns average, min, max and length of numbers", () => {
  expect(analyze([1, 8, 3, 4, 2, 6])).toStrictEqual({
    average: 4,
    min: 1,
    max: 8,
    length: 6,
  });
});
